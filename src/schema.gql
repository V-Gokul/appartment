# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type profile {
  id: String!
  name: String!
  email: String!
  createAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Polls {
  id: Int!
  pollName: String!
  description: String!
  questionType: String!
  postedAt: String!
  pollExpiry: String!
  minOptions: Float!
  maxOptions: Float!
  option: [Option!]!
}

type Option {
  id: Int!
  text: String!
}

type PollSelection {
  id: Int!
}

type noticeBoard {
  id: Int!
  title: String!
  description: String!
  image: String!
  profile_id: profile!
}

type Query {
  profiles: [profile!]!
  profile(id: Int!): profile!
  polls: [Polls!]!
  poll(id: Int!): Polls!
  noticeBoard(id: Int!): noticeBoard!
}

type Mutation {
  createProfile(data: CreateProfileInput!): profile!
  updateProfile(updateProfileInput: UpdateProfileInput!): profile!
  removeProfile(id: Int!): profile!
  createPoll(createPollInput: CreatePollInput!): Polls!
  removePoll(id: Int!): Polls!
  pollselection(data: CreatePollSelectionInput!): PollSelection!
  selection: PollSelection!
  createNoticeBoard(createNoticeBoardInput: CreateNoticeBoardInput!): noticeBoard!
  updateNoticeBoard(id: Int!, data: UpdateNoticeBoardInput!): noticeBoard!
  removeNoticeBoard(id: Int!): noticeBoard!
}

input CreateProfileInput {
  name: String!
  email: String!
}

input UpdateProfileInput {
  name: String
  email: String
  id: Int!
}

input CreatePollInput {
  pollName: String!
  description: String!
  questionType: String!
  postedAt: DateTime!
  pollExpiry: DateTime!
  minOptions: Int!
  maxOptions: Int!
  option: [CreateOptionInput!]
}

input CreateOptionInput {
  text: String!
}

input CreatePollSelectionInput {
  profile_id: Int!
  poll_id: Int!
  option_id: [Int!]!
}

input CreateNoticeBoardInput {
  title: String!
  description: String!
  images: String!
  profile_id: Int!
}

input UpdateNoticeBoardInput {
  title: String
  description: String
  images: String
  profile_id: Int
}
